# ---------------------------------------------------------------------------
# ARC Race - server-side configuration
#
# Copy to .env (repository root) and fill in. Every value here is read by the
# SERVER ONLY. Never prefix any of these with NEXT_PUBLIC_, VITE_, PUBLIC_ or
# similar: those prefixes tell bundlers to inline a value into client code.
# ---------------------------------------------------------------------------

# --- OpenRouter (server secret) --------------------------------------------
# Required for the AI lane. Without it the app still runs; the AI is disabled.
OPENROUTER_API_KEY=

# Any OpenRouter slug that accepts image input, which MNIST-PRO requires.
# One MNIST-PRO round can take up to 36 calls, so cost scales with rounds played.
# A ":free" slug such as google/gemma-4-31b-it:free costs nothing but is rate
# limited, which a long round can hit.
OPENROUTER_MODEL=google/gemini-3.8-flash
# Must fit a full 30x30 answer grid (pretty-printed JSON is ~4 tokens a cell).
OPENROUTER_MAX_OUTPUT_TOKENS=6000

# --- Race rules ------------------------------------------------------------
# Shared countdown for both players, in seconds (30-3600).
RACE_TIME_LIMIT_SECONDS=300
# Submissions allowed per player (1-10). ARC-AGI-1 originally allowed three.
MAX_ATTEMPTS=3
# Show the AI's answers as they land (false = hidden until your run ends).
AI_ANSWERS_LIVE=true

# --- Data ------------------------------------------------------------------
# Leave blank to use the vendored public training set in backend/data/arc1.
ARC_DATA_DIR=

# --- Web -------------------------------------------------------------------
# Comma-separated origins allowed for CORS. Leave blank for same-origin only.
ALLOWED_ORIGIN=
ENVIRONMENT=development

# --- Cost + abuse controls -------------------------------------------------
MAX_CONCURRENT_AI_RACES=2
SESSION_TTL_MINUTES=30
MAX_RACES_PER_IP_PER_HOUR=12
AI_TURN_MIN_INTERVAL_SECONDS=0.35
